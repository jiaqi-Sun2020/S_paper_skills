interface:
  display_name: "LaTeX Paper Pipeline"
  short_description: "Build LaTeX paper pipelines with .bib references, one figures folder, standalone main.tex drafts, and QCT/QWCT frameworks."
  default_prompt: "Use $latex-paper-build-skill to create or restructure a research-paper pipeline with .bib-managed references, a single figures/ folder, and a complete editable main.tex when requested."